class Segment:
    def __init__(segment, name, origin, destination, cost):
        segment.name = name
        segment.origin = origin
        segment.destination = destination
        segment.cost = float(cost)