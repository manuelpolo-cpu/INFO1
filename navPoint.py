class NavPoint:
    def __init__(NavPoint, number, name, latitude, longitude):
        NavPoint.number = number
        NavPoint.name = name
        NavPoint.latitude = latitude
        NavPoint.longitude = longitude